#include<iostream>                            //Input an array
int main(){
    int n;
    std::cout<<"Enter the size of the array: "<<"\n";
    std::cin>>n;
    int arr[n];
    for(int i = 0;i<=n;i++){
        std::cin>>arr[i];
    }
    int target;
    bool found = false;

    std::cout<<"Enter the number to search: "<<"\n";
    std::cin>>target;

    for(int i = 0;i <= n;i++){
        if(arr[i]==target){
            std::cout<<"Found the" <<target<<" at index " << i << "\n"; 
        } found = true;
        break;
    }
if(!found){
    std::cout<<"Target not Found!"<<"\n";
}
return 0;



}